<%= form_for(current_user.active_relationships.build) do |f| %>
  <div><%= hidden_field_tag :following_id, @user.id %></div>
  <%= f.submit "フォロー", class: "btn btn-primary center-block btn-block" %>
<% end %>